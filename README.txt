SANGAI QUIET PREMIUM UI

Replace only these three files in your current GitHub repository:
- index.html
- styles.css
- app.js

Keep your current config.js unchanged.
Do not rerun Supabase SQL.

After GitHub upload, Netlify will redeploy automatically. Then hard refresh the live site.

This redesign preserves the existing HTML IDs and app.js database flows.
